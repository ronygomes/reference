Feature: Cucumber JVM Lambda Support

  Example:

    Cucumber JVM has support for both annotation and methods. Given() vs @Given, When() vs @When, @Then vs Then()
    Also provides hook methods like BeforeAll(), AfterAll(), Before(), After(), BeforeStep(), AfterStep()

    Given some condition as Given lambda
    When When lambda is called
    Then it will same as Then lambda
